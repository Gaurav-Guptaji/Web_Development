

# first time
git init

git status
git add.

# first time
git config --global user.name "Name"
git config --global user.email "email"

git commit -m "Your message"

# first time
git remote remove origin (in case below commnad gives error remote origin already exist)
git remote add origin "repository path"

# Daily Commands
git add .
git commit -m "Daily comment"
git push origin master